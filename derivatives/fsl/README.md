# FSL derivatives

`EVfiles/` is the only source-adjacent derivative collection intentionally retained here. The active generator writes session/run-aware 3-column files. FEAT outputs and rendered subject designs are generated below this directory at runtime but are ignored because they are large and reproducible from Linux2 inputs plus the tracked templates.
