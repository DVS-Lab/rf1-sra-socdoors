# Imaging plot inputs

These text files contain coordinates, extracted values, or mask/result summaries used by historical imaging figures. They are result/figure assets rather than production FEAT inputs. Names encode task, contrast, model, statistic, or mask when known; exact generating commands are not consistently recorded and therefore need confirmation before reuse.

Related result maps and extractions are retained under `derivatives/imaging_plots/`.
